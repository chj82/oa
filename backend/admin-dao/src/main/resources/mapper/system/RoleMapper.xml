<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.oa.dao.system.RoleMapper">
    <select id="selectByIdForUpdate" resultType="com.oa.entity.system.RoleEntity">
        SELECT id, code, name, description, status, created_at, updated_at
        FROM t_role
        WHERE id = #{id}
        FOR UPDATE
    </select>

    <select id="selectByIdsForUpdate" resultType="com.oa.entity.system.RoleEntity">
        SELECT id, code, name, description, status, created_at, updated_at
        FROM t_role
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        ORDER BY id
        FOR UPDATE
    </select>
</mapper>
