<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.oa.dao.system.SystemVersionMapper">
    <select id="selectVersion" resultType="long">
        SELECT version_value
        FROM t_system_version
        WHERE version_code = #{versionCode}
    </select>

    <update id="incrementVersion">
        UPDATE t_system_version
        SET version_value = version_value + 1,
            updated_at = #{updatedAt}
        WHERE version_code = #{versionCode}
    </update>
</mapper>
